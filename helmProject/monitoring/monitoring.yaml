grafana:
  service:
    type: NodePort
    nodePort: 30000
    port: 80
  adminUser: admin
  adminPassword: prom-operator
  serviceMonitor:
    enabled: true

prometheus:
  service:
    type: NodePort
    nodePort: 30090
    port: 9090

alertmanager:
  service:
    type: NodePort
    nodePort: 30095
    port: 9093

prometheusOperator:
  admissionWebhooks:
    enabled: false

prometheus:
  prometheusSpec:
    scrapeInterval: 15s